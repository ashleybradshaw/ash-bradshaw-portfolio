/** Shared type tokens so body copy stays readable across pages. */

/** ~65 characters: comfortable line length for editorial blocks. */
export const typeMeasure = "max-w-[65ch]";

/** Body copy: 16px, 1.7 leading, 65ch measure. */
export const typeBody =
  "max-w-[65ch] font-sans text-base font-bold leading-[1.7] tracking-[-0.01em]";

/** Card / compact body where the parent already constrains width. */
export const typeBodyFlush =
  "font-sans text-base font-bold leading-[1.7] tracking-[-0.01em]";

/** Section deck titles (H2/H3 visual size). */
export const typeDeck =
  "font-display text-[28px] font-bold uppercase leading-9 tracking-[-0.01em]";

/** Small uppercase labels. */
export const typeKicker =
  "font-sans text-base font-bold uppercase leading-6 tracking-[-0.01em]";
